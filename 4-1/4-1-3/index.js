// Объекты-константы?

const user = {
  name: "John"
};

// это будет работать?
user.name = "Pete";

/**
 * Можно изменить объект, объявленный с помощью const
 * const запрещает телько переприсваивание
 */